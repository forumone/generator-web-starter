# Values with a . are here so that WP Starter knows to retrieve them from the env.

WORDPRESS_ENV=.

DB_HOST=.
DB_NAME=.
DB_USER=.
DB_PASSWORD=.

WP_HOME=.

# Don't allow WP to write to the filesystem (it can't in a container anyway.)
DISALLOW_FILE_MODS=true
DISALLOW_FILE_EDIT=true
